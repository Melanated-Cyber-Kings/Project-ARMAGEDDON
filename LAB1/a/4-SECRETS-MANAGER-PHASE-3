






Purpose

Store database credentials securely outside of code and infrastructure, so the EC2 instance can retrieve them dynamically at runtime.

This prevents hardcoding passwords and establishes the identity-based trust model used in real AWS environments.
